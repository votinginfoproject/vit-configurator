(ns vit-configurator.db)

(def default-db
  {:preview-ready false
   :official-only true
   :logo {:type :default}
   :size :responsive
   :config {:logo {:type :default}
            :official-only true
            :size :responsive}})
