(ns vit-configurator.db)

(def default-db
  {:preview-ready false
   :logo {:type :default}
   :language :none
   :official-data-only true
   :links {:en {}}
   :size :responsive
   :title {:en "Voting Information Tool"}})
